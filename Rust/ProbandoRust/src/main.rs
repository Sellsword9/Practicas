#![allow(unsed)]
use std::io; 
use rand::Rng; //Para generar numeros aleatorios
use std::io::{Write, BufReader, BufRead, ErrorKind};
use std::fs::File;

fn main() {
    println!("Dime tu nombre");
    
    //Inicia  mutable  identificador: 
    let       mut      nombre:        
    //tipo = valor
    String = String::new();

    // En una línea:
    let mut nombre: String = String::new();


    println!("Hello, world!");
}
